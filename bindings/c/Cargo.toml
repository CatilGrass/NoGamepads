[package]
name = "nogamepads-c"
edition = "2024"
version.workspace = true

[dependencies]
nogamepads-core = { path = "../../core" }

[build-dependencies]
cbindgen = "0.29.0"

[lib]
name = "nogamepads_lib_c"
crate-type = [ "cdylib", "staticlib"]