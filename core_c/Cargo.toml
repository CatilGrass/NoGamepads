[package]
name = "nogamepads_c"
edition = "2024"

[dependencies]
nogamepads-core = { path = "../core" }

[build-dependencies]
cbindgen = "0.29.0"

[lib]
name = "nogamepads_lib_c"
crate-type = [ "cdylib", "staticlib"]